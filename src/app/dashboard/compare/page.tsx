import { ComingSoonScreen } from '@/components/dashboard/ComingSoonScreen';

export default function ComparePerformancePage() {
  return (
    <ComingSoonScreen
      icon="git-compare"
      title="Compare Performance"
      subtitle="Line up any two mocks side by side."
      note="Pick two mocks and see where marks were gained or lost between them."
    />
  );
}
